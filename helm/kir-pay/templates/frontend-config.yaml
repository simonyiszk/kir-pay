{{ if .Values.frontend.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-frontend
  name: {{ .Release.Name }}-frontend-config
data:
  VITE_BACKEND_URL: "https://{{ .Values.app.host }}"
{{ end }}
